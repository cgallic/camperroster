{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_comment": "Agent branches push work-in-progress commits often, and each one queued a preview build. Production and every human branch still deploy normally.",
  "ignoreCommand": "[ \"$VERCEL_ENV\" = \"production\" ] && exit 1; case \"$VERCEL_GIT_COMMIT_REF\" in claude/*) exit 0;; esac; exit 1"
}
